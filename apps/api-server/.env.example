# Server binding
PORT=9700
HOST=0.0.0.0

# Authentication — set to "token" to require bearer auth, or omit for open access
# TELEMETRY_AUTH_MODE=token
# TELEMETRY_AUTH_TOKEN=your-secret-token-here

# Heartbeat timeout — mark machine offline after this silence (ms)
# HEARTBEAT_TIMEOUT_MS=120000
